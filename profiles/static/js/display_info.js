let infoPElements = document.getElementsByClassName('display-info');
let infoDiv = document.getElementById('info-div');

if (infoPElements.length = 0) {
    infoDiv.text = "You currently have no information saved with us."
}